<%= form_with(model: @tracker, local: true) do |f| %>
  <div data-controller="tracker-form" class="tracker-form-modal">
    <div class="tracker-form-container">
      <h2>Tes habits du jour</h2>
      <div class="habits-name-container">
        <% @habits.each do |habit| %>

            <div class="d-flex justify-content-center"><%= habit.name %></div>
            <div class="d-flex justify-content-center"><%= habit.goal&.value %> <%= habit.habit_type&.unit %></div>

        <% end %>
      </div>
    </div>
  </div>
  <div class="dashboard-bottom d-flex justify-content-center">
  <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#trackerFormModal">Mettre à jour</button>
</div>
<% end %>
